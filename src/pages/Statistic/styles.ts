import styled from 'styled-components';

type Props = {};

export default styled.section<Props>`
  .container {
    padding: 30px;
    .text-label {
      font-size: 13px;
      color: #777777;
    }
  }
`;
